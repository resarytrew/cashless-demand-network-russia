# Aborted preflight — not evidence

The first protocol gate invocation used the decomposition config rather than a protocol-gate
config and stopped on missing `robustness.gate_seed` before perturbation or optimization.
This directory contains no accepted experiment. The successful fresh protocol gate is
`outputs/round16_protocol_gate`; the sensitivity experiment is
`outputs/round16_perturbation_decomposition`. Partial preflight files are preserved for traceability.
